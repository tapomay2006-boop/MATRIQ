"""Training and evaluation for the Siamese reranker. Never imported by a request.

    pairs.py          labelled material pairs: read them, and generate them
                      from a standard-format corpus
    train_siamese.py  the training loop
    evaluate.py       Qdrant-only against Qdrant + Siamese, on real queries

Run through scripts/train_siamese.py and scripts/evaluate_search.py.
"""
