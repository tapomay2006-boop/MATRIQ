fastapi>=0.118.0
uvicorn[standard]>=0.37.0
pydantic>=2.11.0
pydantic-settings>=2.10.0
python-multipart>=0.0.9
sqlalchemy>=2.0.43
aiosqlite>=0.20.0
greenlet>=3.0.0
asyncpg>=0.30.0
httpx>=0.28.0
numpy>=2.0.0
qdrant-client>=1.12.0

# POST /extract/csv reads CSV and XLSX. Same versions pipeline-one pins.
pandas>=2.2.0
openpyxl>=3.1.0
pytest>=8.4.0
pytest-asyncio>=1.2.0
ruff>=0.14.0
mypy>=1.18.0
torch>=2.4.0
transformers>=4.51.0
peft>=0.13.0
accelerate>=1.0.0

# 4-bit NF4 quantisation, which is what keeps the adapter inside ~2 GB of VRAM.
# CUDA only; on CPU the engine falls back to bfloat16 and does not need it.
bitsandbytes>=0.44.0; platform_system != "Darwin"
